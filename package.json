{
  "scripts": {
    "dev": "concurrently \"uv run --project backend uvicorn main:app --reload --app-dir backend\" \"npm run dev --workspace frontend\" "
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
